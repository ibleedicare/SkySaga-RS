[package]
name = "skysaga-proto"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "RakNet wire formats for the SkySaga game protocol: BitStream, packets, component data."

[dependencies]
skysaga-core = { workspace = true }
thiserror = { workspace = true }

[lints.rust]
unsafe_code = "forbid"

[dev-dependencies]
skysaga-world = { workspace = true }
