[package]
name = "raknet-sys"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Raw FFI declarations for the handful of SLikeNet entrypoints the emulator needs."
links = "RakNet"
build = "build.rs"
